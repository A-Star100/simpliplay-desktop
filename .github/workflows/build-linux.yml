name: Build Linux

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    paths-ignore:
      - '.github/workflows/dependabot-bump.yml'
      - '.github/workflows/dependabot.yml'
      - 'README.md' #ignore readme
      - '.gitignore' #ignore the gitignore that is the file that ignores other files
  workflow_dispatch:
  workflow_call:

jobs:
  build_x64:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 22

      - name: Install project deps
        run: npm install
        working-directory: simpliplay

      - name: Build Snap and AppImage
        run: npx electron-builder --linux snap AppImage --x64
        working-directory: simpliplay

      - name: Install Flatpak deps
        run: |
          # add flatpak ppa
          # for latest flatpak pkgs
          sudo add-apt-repository ppa:flatpak/stable
          sudo apt-get update
          sudo apt-get install -y build-essential flatpak
          sudo apt-get install -y appstream appstream-compose appstream-util
          # setup flathub repo
          flatpak remote-add --user --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
        #continue-on-error: true
        
      - name: Build Flatpak
        run: |
          # now build
          flatpak-builder --user --disable-rofiles-fuse --install-deps-from=flathub --force-clean build-dir flatpak/me.anirudhsevugan.SimpliPlay.yml
          flatpak build-export repo build-dir
          flatpak build-bundle repo SimpliPlay-x64-linux.flatpak me.anirudhsevugan.SimpliPlay
          mv *.flatpak dist/
        working-directory: simpliplay
        #continue-on-error: true

      - uses: actions/upload-artifact@v6
        with:
          name: linux-x64-snap
          path: simpliplay/dist/*.snap

      - uses: actions/upload-artifact@v6
        with:
          name: linux-x64-appimage
          path: simpliplay/dist/*.AppImage
          
      - uses: actions/upload-artifact@v6
        with:
          name: linux-x64-flatpak
          path: simpliplay/dist/*.flatpak
        continue-on-error: true

  build_arm64:
    runs-on: ubuntu-22.04-arm
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 22

      - name: Use arm64 package.json instead of x64
        run: |
          mv simpliplay/package.json simpliplay/package-unused.json
          mv simpliplay/package-arm64-linux.json simpliplay/package.json

      - name: Install project deps
        run: npm install
        working-directory: simpliplay

      - name: Build AppImage
        run: npx electron-builder --linux AppImage --arm64
        working-directory: simpliplay

      # the below is to prevent snapcraft from crashing
      # for some reason it does not cope without these deps
      # on arm64 and armv7 anyway

      - name: Update package listings and install libgdk-pixbuf
        run: sudo apt-get update && sudo apt-get install --only-upgrade libgdk-pixbuf-2.0-0

      - name: Install Snapcraft CLI
        run: sudo snap install snapcraft --classic

      - name: Install GNOME packages
        run: sudo snap install gnome-3-28-1804

      - name: Install GTK theme snap
        run: sudo snap install gtk-common-themes

      - name: Install core22 snap
        run: sudo snap install core22

      # this uses SNAPCRAFT_BUILD_ENVIRONMENT=host
      # cuz otherwise the runner tries to use multipass sandboxing
      # which doesn't work correctly on GH actions runners (for arm64 anyway)
      # USE_SYSTEM_FPM=true is kinda self explanatory but the gist is the same
      # it ditches virtualization
      # cool... to anyone who's debugging don't edit this though
      - name: Build Snap
        run: SNAPCRAFT_BUILD_ENVIRONMENT=host USE_SYSTEM_FPM=true npx electron-builder --linux snap --arm64
        working-directory: simpliplay
        #continue-on-error: true
      - name: Install Flatpak libs
        run: |
          # add flatpak ppa
          # for latest flatpak pkgs
          sudo add-apt-repository ppa:flatpak/stable
          sudo apt-get update
          sudo apt-get install -y build-essential flatpak flatpak-builder
          sudo apt-get install -y appstream appstream-compose appstream-util
          # setup flathub repo
          flatpak remote-add --user --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
      - name: Build Flatpak
        run: |
          # now build
          flatpak-builder --user --disable-rofiles-fuse --install-deps-from=flathub --force-clean build-dir flatpak/me.anirudhsevugan.SimpliPlay.yml
          flatpak build-export repo build-dir
          flatpak build-bundle repo SimpliPlay-arm64-linux.flatpak me.anirudhsevugan.SimpliPlay
          mv *.flatpak dist/
        working-directory: simpliplay
        #continue-on-error: true

      - uses: actions/upload-artifact@v6
        with:
          name: linux-arm64-appimage
          path: simpliplay/dist/*.AppImage
          
      - uses: actions/upload-artifact@v6
        with:
          name: linux-arm64-snap
          path: simpliplay/dist/*.snap

      - uses: actions/upload-artifact@v6
        with:
          name: linux-arm64-flatpak
          path: simpliplay/dist/*.flatpak

  build_armv7:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
  
      - uses: actions/setup-node@v6
        with:
          node-version: 22

      # qemu isn't needed
      # cuz when building prebuilt
      # electron binaries are already installed
      # so there is literally no reason
      # to use qemu anymore
      # and also npx electron builder forces armv7 snap builds
      # and just to double check the file header's correct for armv7
      # so neat

      - name: Install dependencies
        run: npm install
        working-directory: simpliplay

      - name: Build AppImage
        run: npx electron-builder --linux AppImage --armv7l
        working-directory: simpliplay

      # the below is to prevent snapcraft from crashing
      # for some reason it does not cope without these deps
      # on arm64 and armv7 anyway

      - name: Update package listings and install libgdk-pixbuf
        run: sudo apt-get update && sudo apt-get install --only-upgrade libgdk-pixbuf-2.0-0

      - name: Install Snapcraft CLI
        run: sudo snap install snapcraft --classic

      - name: Install GNOME packages
        run: sudo snap install gnome-3-28-1804

      - name: Install GTK theme stuff
        run: sudo snap install gtk-common-themes

      - name: Install core22 snap
        run: sudo snap install core22

      - name: Build snap
        run: npx electron-builder --linux snap --armv7l
        working-directory: simpliplay

      # - name: Install Flatpak libs
      #   run: |
      #     sudo apt-get update
      #     # build-essential will be useful if I ever add native c++ modules
      #     # but otherwise it won't do much
      #     sudo apt-get install -y build-essential flatpak flatpak-builder
      #   continue-on-error: true

      # # the patch below's needed cuz freedesktop runtime dropped
      # # support for armv7 after the 19.08 runtime
      # # but the electron baseapp and older freedesktop runtime
      # # both still have versions for it so patching should work fine
      # - name: Patch manifest
      #   run: sed -i "s/'25.08'/'19.08'/g" flatpak/me.anirudhsevugan.SimpliPlay.yml # sed is so annoying sometimes
      #   working-directory: simpliplay
        
      # - name: Build Flatpak
      #   run: |
      #     # setup flathub repo
      #     flatpak remote-add --user --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
      #     # now build
      #     flatpak-builder --user --install-deps-from=flathub --arch=arm --force-clean build-dir flatpak/me.anirudhsevugan.SimpliPlay.yml
      #     flatpak build-export repo build-dir
      #     flatpak build-bundle repo SimpliPlay-armv7-linux.flatpak me.anirudhsevugan.SimpliPlay
      #     mv *.flatpak dist/
      #   working-directory: simpliplay
      #   continue-on-error: true
        
      - uses: actions/upload-artifact@v6
        with:
          name: linux-armv7l-v2-snap
          path: simpliplay/dist/*.snap

      - uses: actions/upload-artifact@v6
        with:
           name: linux-armv7-appimage
           path: simpliplay/dist/*.AppImage

      # - uses: actions/upload-artifact@v6
      #   with:
      #      name: linux-armv7-flatpak
      #      path: simpliplay/dist/*.flatpak

  # build_i386_unstable:
  #   runs-on: ubuntu-slim
  #   steps:
  #     - uses: actions/checkout@v6
  
  #     - uses: actions/setup-node@v6
  #       with:
  #         node-version: 22

  #     - name: Patch package.json
  #       run: |
  #         # save og package.json
  #         cp simpliplay/package.json simpliplay/package.json.orig
      
  #         # is jq installed?
  #         if ! command -v jq &> /dev/null
  #         then
  #           echo "jq not found, installing..."
  #           sudo apt-get update
  #           sudo apt-get install -y jq
  #         fi
      
  #         # patch package.json to use Electron v3.1
  #         # might be useful for debugging or checking on i386
  #         # but the app uses functions that might be too new for electron v3
  #         # crashes on electron2 but haven't tested v3 properly yet
  #         jq '.devDependencies.electron="3.1.13"' simpliplay/package.json.orig > simpliplay/package.i386.json
      
  #         # replace package.json for the i386 build
  #         mv simpliplay/package.i386.json simpliplay/package.json
  
  #     - run: npm install
  #       working-directory: simpliplay
  
  #     - name: Build AppImage
  #       run: npx electron-builder --linux AppImage --ia32 && mv simpliplay/dist/*.AppImage simpliplay/dist/SimpliPlay-linux-i386.AppImage.unstable
  #       working-directory: simpliplay
  
  #     - uses: actions/upload-artifact@v6
  #       with:
  #         name: linux-i386-unstable-appimage
  #         path: simpliplay/dist/*.AppImage.unstable

  # start-linux-tests:
  #   name: Start Linux Tests
  #   needs: [build_x64, build_arm64, build_armv7]
  #   uses: ./.github/workflows/test-linux.yml
